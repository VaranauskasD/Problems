// Return the index of the first occurrence of a string in a string. Return 0 if the string is empty or -1 if it does not occur.
const strStr = (haystack, needle) => (needle ? haystack.indexOf(needle) : 0)
